# Core requirements cho BlastBot (Simplified)
discord.py>=2.0.0
python-dotenv>=0.19.0

# Optional: Uncomment if using SQLAlchemy (not needed for current SQLite setup)
# SQLAlchemy>=1.4.0
# aiosqlite>=0.17.0